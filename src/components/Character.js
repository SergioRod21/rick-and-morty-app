import React from 'react'
import Card from './Card';
import App from '../App';
import { actualId } from './Card';
import { useLocation, Link, BrowserRouter, Routes, Route} from 'react-router-dom';

function Character ()  {
 



  // Utilizar el valor de id en el componente
  return (
    <BrowserRouter>
      <Routes>
        
      </Routes>
  </BrowserRouter>
  );
}
export default Character